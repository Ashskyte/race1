package com.practice.race1.startrace;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// SPRING[COMPONENT SCAN] & SPRING BOOT[ AUTOCONFIGURATION]
@SpringBootApplication
public class StartraceApplication {

	public static void main(String[] args) {
		SpringApplication.run(StartraceApplication.class, args);
	}

}
